# 프로젝트 지침 (CLAUDE.md)

이 폴더는 사용자의 개인 웹사이트 프로젝트입니다.
앞으로 이 폴더에서 작업할 때는 **항상 아래 지침을 지켜서** 작업합니다.

## 작업 환경

- **사용자는 코딩을 모르는 비개발자입니다.** 모든 설명은 쉬운 한국어로 해줍니다. 전문 용어는 풀어서 설명합니다.
- **도구 구성**: Claude 데스크톱 앱(Claude Code)으로 파일 작업 → GitHub Desktop으로 Commit·Push → Vercel이 GitHub을 감지해 자동 배포하는 구조입니다.
- **개발 도구 없음**: 사용자 컴퓨터에는 터미널이나 Node.js 같은 개발 도구가 없습니다.

## 지켜야 할 규칙

1. **설치(npm)나 빌드가 필요한 프레임워크(React, Next.js 등)는 절대 쓰지 않습니다.**
2. **순수 HTML / CSS / JavaScript로만 작업합니다.**
3. **파일을 더블클릭하면 브라우저에서 바로 열려야 합니다.** (별도 실행 명령 없이)
4. **첫 화면 파일 이름은 `index.html`로 합니다.**

## 앞으로의 계획 (4주 로드맵)

이 사이트는 4주에 걸쳐 발전시킵니다.

- **1~2주차**: 순수 HTML/CSS/JS로 기본 사이트 제작
- **3주차**: Supabase DB 연동
- **4주차**: Claude API를 쓰는 AI 기능(`/api`) 추가

> 참고: 3주차(Supabase), 4주차(Claude API) 기능은 Vercel 배포 환경에서 동작하도록 만들 예정입니다.
> 그때까지는 위의 "순수 HTML/CSS/JS" 원칙을 유지합니다.
