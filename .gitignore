# Windows / macOS 시스템 파일
Thumbs.db
Desktop.ini
.DS_Store

# 클로드 코드 로컬 설정
.claude/

# 사내 규정·서식 문서 (공개 저장소에 노출 금지 — 챗봇 지식으로만 사용)
*.pdf
*.hwp
*.hwpx
*.doc
*.docx
*.xls
*.xlsx
*.ppt
*.pptx

# 학습 데이터 (사내 정보 · 저장소 노출 금지)
training_data/*
!training_data/README.md

# Claude Code MCP 로컬 설정 (환경별로 다를 수 있음)
.mcp.json
